
INJECT:law_religious_schools = {
	institution_modifier = {
		state_education_access_wealth_add					= 0.001	# Added
	}
	on_activate = {
		random_law = { limit = { type = law_type:law_religious_schools }	save_scope_as = relevant_education_law }
		effect_inherit_apprenticeships = yes
	}
}
INJECT:law_private_schools = {
	institution_modifier = {
		state_conversion_mult								= 0.05	# Added - Ideally schools would be entirely unbiased, but that's hardly realistic.
	}
	on_activate = {
		random_law = { limit = { type = law_type:law_private_schools }	save_scope_as = relevant_education_law }
		effect_inherit_apprenticeships = yes
	}
}
INJECT:law_public_schools = {
	institution_modifier = {
		state_assimilation_mult								= 0.075	# Increased (12.5% -> 20%)
		state_education_access_add							= 0.075	# Increased (12.5% -> 20%)
		state_conversion_mult								= 0.15	# Added - Ideally schools would be entirely unbiased, but that's hardly realistic.
		state_education_access_wealth_add					= 0.001	# Added - Even if public schooling is available, more money makes it easier still.
	}
	on_activate = {
		random_law = { limit = { type = law_type:law_public_schools }	save_scope_as = relevant_education_law }
		effect_inherit_apprenticeships = yes
	}
}