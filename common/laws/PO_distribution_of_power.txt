# Distrubution of Power
INJECT:law_technocracy = {
	on_activate = { if = { limit = { trigger_junker_landowners = yes }		add_modifier	= junker_compromise } }	# TODO: Turn this into an amendment
	on_deactivate = { if = { limit = { has_modifier = junker_compromise }	remove_modifier	= junker_compromise } }	# TODO: Turn this into an amendment
	modifier = {
		country_legitimacy_ideological_incoherence_mult			=-0.25
		country_academics_pol_str_mult							= 0.17
		country_engineers_pol_str_mult							= 0.17
		country_officers_pol_str_mult							= 0.17
		interest_group_ig_intelligentsia_pop_attraction_mult	= 0.10
		interest_group_ig_industrialists_pop_attraction_mult	= 0.10
		interest_group_ig_armed_forces_pop_attraction_mult		= 0.10
	}
	ai_impose_chance = {
		if = {	# Added to make BIC runs more fun
			limit = {
				scope:target_country = {
					c:BIC ?= this
					has_law_or_variant = law_type:law_oligarchy
					has_technology_researched = central_archives
				}
			}
			add = 1000
		}
	}
}
INJECT:law_anarchy = {
	unlocking_laws = { law_chiefdom law_parliamentary_republic }
}