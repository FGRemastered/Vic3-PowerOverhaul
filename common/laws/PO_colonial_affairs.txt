REPLACE:law_frontier_colonization = {
	group = lawgroup_colonization
	icon = "gfx/interface/icons/law_icons/frontier_colonization.dds"
	progressiveness = 25
	unlocking_technologies = { colonization }
	can_enact = {
		OR = {
			AND = {
				country_is_colonial_or_company = yes
				any_decentralized_country = { is_adjacent_to_country = ROOT }
			}
			AND = {	# Added
				country_is_colonial_or_company = no
				any_scope_state = { NOT = { is_homeland_of_country_cultures = ROOT } }
			}
			any_scope_state = {
				is_homeland_of_country_cultures = ROOT
				any_neighbouring_state = { OWNER = { is_country_type = decentralized } }
			}
		}
	}
	institution = institution_colonial_affairs
	modifier = { state_migration_pull_unincorporated_mult				= 1 }	# Changed - It's much easier to convince someone to move to a new state if they can do it by themselves.
	institution_modifier = {
		interest_group_ig_rural_folk_approval_add						= 1
		interest_group_ig_petty_bourgeoisie_approval_add				= 1
	}
	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_colonization_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}
	ai_will_do = {
		NOR = {
			has_law_or_variant = law_type:law_colonial_exploitation
			has_law_or_variant = law_type:law_colonial_resettlement
		}
	}
	ai_enact_weight_modifier = {	# Petitions
		value = 0
		if = { limit = { ai_has_enact_weight_modifier_journal_entries = yes } add = 750 }
	}
	ai_impose_chance = {
		value = 0
		if = {
			limit = {
				OR = {
					has_law_or_variant = law_type:law_colonial_resettlement
					has_law_or_variant = law_type:law_frontier_colonization
				}
				scope:target_country = { has_law_or_variant = law_type:law_no_colonial_affairs }
				power_bloc ?= { has_principle_group = principle_group:principle_group_colonial_offices }
			}
			add = 10
		}
		if = { limit = { trigger_BIC_british_dictate = yes } multiply = 0 }
	}
}
INJECT:law_colonial_resettlement = {
	institution_modifier = {
		interest_group_ig_landowners_approval_add						= 1
		interest_group_ig_petty_bourgeoisie_approval_add				= 1
	}
}
INJECT:law_colonial_exploitation = {
	modifier = {
		building_group_bg_heavy_industry_unincorporated_throughput_add	=-0.30	# Base 20% +30%
		building_group_bg_plantations_unincorporated_throughput_add		= 0.05	# Plantations, but not agriculture
		building_group_bg_greenhouses_unincorporated_throughput_add		=-0.25	# Cancels out Plantations buff
		building_group_bg_extraction_unincorporated_throughput_add		= 0.25	# Replaced others
		building_group_bg_rubber_unincorporated_throughput_add			=-0.20	# Originally doesn't include fishing and whaling for some reason
		building_group_bg_logging_unincorporated_throughput_add			=-0.20	# Originally doesn't include fishing and whaling for some reason
		building_group_bg_mining_unincorporated_throughput_add			=-0.20	# Originally doesn't include fishing and whaling for some reason
		state_migration_pull_unincorporated_mult						=-0.25	# Added
	}
	institution_modifier = {
		interest_group_ig_industrialists_approval_add					= 1
		interest_group_ig_petty_bourgeoisie_approval_add				= 1
		country_infamy_generation_against_unrecognized_mult				=-0.05	# From Colonial Offices
	}
}